Place optional audio files here for opt-in sound features:
- soft-rain.mp3   (ambient loop)
- soft-pop.mp3    (message chime)

Both sounds are optional.
If files are missing, the app still works and uses a synthesized fallback for chime.